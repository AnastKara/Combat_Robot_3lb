# Large video files - keep locally, don't track in repo
images/*.MOV
images/*.mov
images/*.mp4

# High-res originals (compressed versions are committed instead)
images/*.JPEG

# Tool artifacts
.qodo/

# Frame extraction scratch directory
images/_frames/
